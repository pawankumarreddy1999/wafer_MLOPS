from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='To predict whether the wafer needs to be change or not.',
    author='pavan',
    license='MIT',
)
